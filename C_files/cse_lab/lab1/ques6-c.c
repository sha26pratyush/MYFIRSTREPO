#include<stdio.h>
#include<math.h>

int main() {

float T, a, b, c;
	printf("Enter the value of a, b and c\n");
	scanf("%f%f%f", &a, &b, &c);

T = 2*a + sqrt(b) + 9*c ;

	printf("The value of T is %f\n", T);

return 0;
}
